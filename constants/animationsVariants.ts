export const defaultAnimationVariant = {
    initial: { opacity: 0, y: 100 },
    whileInView: { opacity: 1, y: 0 },
    viewport: { once: true },
    transition: {
        delay: 0,
        duration: 0.3,
    }
}

export const navbarAnimationVariant = {
    initial: { opacity: 0},
    whileInView: { opacity: 1},
    viewport: { once: true },
    transition: {
        delay: 0,
        duration: 0.3,
    }
}
